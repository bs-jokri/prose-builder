# Prose Builder

A simple prose builder for constructing text content from individual sentences.

## Overview
Prose Builder provides a flexible way to construct text content by registering and combining sentences.

## Features
1. Build text content by registering individual sentences
2. Automatic line break handling between sentences
3. Simple interface-based design with `Prose` and `Sentence` interfaces


